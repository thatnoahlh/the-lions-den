#include "modelLoader.h"
#include <iostream>

// Example implementation of loadModel
unsigned int loadModel(const std::string& path) {
    std::cout << "Loading model from: " << path << std::endl;

    // Placeholder: Replace this with actual model loading logic (e.g., using Assimp)
    unsigned int modelID = 1; // Dummy value
    return modelID;
}